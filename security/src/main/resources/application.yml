server:
  port: 8086
  
spring:
  application:
    name: SECURITY-SERVICE
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://localhost:8084/eureka/
  instance:
    hostname: localhost
    
 